/* ==========================================================================
Programa CAN · Tablero ejecutivo — estilos
Paleta tomada de las presentaciones de Grupo Consupago, con predominio
de azules claros. Sin frameworks.
========================================================================== */

:root {
--navy: #1C355E;
--navy-2: #2A4A7B;
--sky: #7BAAE0;
--sky-2: #A9C8EC;
--sky-3: #CFE1F6;
--mist: #E4EEFA;
--ice: #F3F7FC;
--line: #D3E2F4;
--line-soft: #E8EFF8;
--surface: #FFFFFF;
--ink: #1B2A44;
--ink-2: #44546E;
--ink-3: #5F6F87;
--ember: #E8561C;
--brick: #9C3A1F;
--leaf: #3F8A3A;

/* Color por mesa (igual que la matriz de responsabilidades) */
--m-programa: #7BAAE0;
--m-decision: #9C3A1F;
--m-otorgante: #E8561C;
--m-ordenante: #4F86C6;
--m-habilitadores: #1C355E;

--font-serif: "Merriweather", Georgia, "Times New Roman", serif;
--font-sans: "Source Sans 3", "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;

--r-s: 6px;
--r-m: 12px;
--r-l: 20px;
}

[data-mesa="programa"] { --mesa: var(--m-programa); }
[data-mesa="decision"] { --mesa: var(--m-decision); }
[data-mesa="otorgante"] { --mesa: var(--m-otorgante); }
[data-mesa="ordenante"] { --mesa: var(--m-ordenante); }
[data-mesa="habilitadores"] { --mesa: var(--m-habilitadores); }

/* ---------- Base ---------- */
*, *::before, *::after { box-sizing: border-box; }
html { color-scheme: light; -webkit-text-size-adjust: 100%; }
body {
margin: 0;
font-family: var(--font-sans);
font-size: 1rem;
line-height: 1.55;
color: var(--ink);
background: var(--ice);
}
h1, h2, h3, h4, h5 {
font-family: var(--font-serif);
color: var(--navy);
line-height: 1.3;
margin: 0 0 .5em;
font-weight: 700;
}
h2 { font-size: clamp(1.45rem, 1.2rem + 1vw, 1.95rem); }
h3 { font-size: 1.2rem; }
h4 { font-size: 1.05rem; }
h5 { font-size: .95rem; }
p { margin: 0 0 .75em; }
ul, ol { margin: 0; }
a { color: #1F5FA8; text-underline-offset: 2px; }
a:hover { color: var(--navy); }
code { font-size: .9em; background: var(--mist); padding: .05em .3em; border-radius: 4px; }
button { font: inherit; }
:focus-visible { outline: 3px solid #2F7FD6; outline-offset: 2px; border-radius: 4px; }
[hidden] { display: none !important; }

.wrap { max-width: 1240px; margin: 0 auto; padding: 0 clamp(1rem, 3vw, 2rem); }
.visually-hidden {
position: absolute !important; width: 1px; height: 1px; padding: 0; margin: -1px;
overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; border: 0;
}
.skip {
position: absolute; left: -999px; top: 0; z-index: 100;
background: var(--navy); color: #fff; padding: .6rem 1rem; border-radius: var(--r-s);
}
.skip:focus { left: 1rem; top: 1rem; color: #fff; }
.notice {
margin: 1.5rem 0; padding: 1rem 1.25rem; border-radius: var(--r-m);
background: #FBE1DA; color: #7A2210; border: 1px solid #F1B9A8;
}
.meta { color: var(--ink-3); font-size: .9rem; }
.lead { color: var(--ink-2); max-width: 70ch; }

/* ---------- Encabezado ---------- */
.masthead { background: var(--mist); border-top: 6px solid var(--navy); }
.masthead__inner {
display: flex; flex-wrap: wrap; align-items: flex-end; justify-content: space-between;
gap: 1rem 2rem; padding-top: 1.1rem; padding-bottom: 1rem;
}
.brand {
display: flex; align-items: center; gap: .5rem; margin: 0 0 .15rem;
font-weight: 700; font-size: .95rem; color: var(--navy);
}
.brand__dot {
width: .8rem; height: .8rem; border-radius: 50%;
border: 3px solid var(--sky); position: relative;
}
.brand__dot::after {
content: ""; position: absolute; right: -4px; top: -4px;
width: .35rem; height: .35rem; border-radius: 50%; background: var(--ember);
}
.masthead h1 { font-size: clamp(1.35rem, 1.1rem + 1vw, 1.85rem); margin: 0; }
.masthead__sub {
font-family: var(--font-sans); font-weight: 400; color: var(--ink-2);
font-size: .6em; margin-left: .35rem;
}
.masthead__meta { display: flex; align-items: center; gap: 1rem; flex-wrap: wrap; font-size: .9rem; color: var(--ink-2); }
.masthead__meta p { margin: 0; text-align: right; line-height: 1.4; }

.btn {
display: inline-flex; align-items: center; gap: .4rem; cursor: pointer;
border-radius: 999px; padding: .5rem 1rem; font-weight: 600; font-size: .9rem;
border: 1px solid var(--navy); background: var(--navy); color: #fff;
}
.btn--ghost { background: transparent; color: var(--navy); border-color: var(--sky); }
.btn--ghost:hover { background: var(--sky-3); }

/* ---------- Pestañas ---------- */
.tabs-wrap {
position: sticky; top: 0; z-index: 30;
background: rgba(228, 238, 250, .96);
border-bottom: 1px solid var(--line);
backdrop-filter: blur(6px);
}
.tabs {
display: flex; gap: .3rem; overflow-x: auto; padding: .55rem 0;
scrollbar-width: thin; scrollbar-color: var(--sky-2) transparent;
}
.tab {
flex: 0 0 auto; cursor: pointer; white-space: nowrap;
border: 1px solid transparent; background: transparent; color: var(--navy);
font-weight: 600; font-size: .95rem; line-height: 1.2;
padding: .55rem .95rem; border-radius: 999px;
transition: background-color .15s ease;
}
.tab:hover { background: var(--sky-3); }
.tab[aria-selected="true"] { background: var(--sky); border-color: var(--sky); color: var(--navy); }

/* ---------- Paneles y bloques ---------- */
.panel { padding: 1.75rem 0 3rem; }
.panel:focus { outline: none; }
.panel__head { margin-bottom: 1.25rem; }
.panel__head p { color: var(--ink-2); max-width: 75ch; }

.block {
background: var(--surface);
border: 1px solid var(--line);
border-radius: var(--r-m);
padding: clamp(1rem, 2vw, 1.5rem);
margin-top: 1.25rem;
}
.block--now { background: #EEF5FD; border-color: var(--sky-2); margin-top: 0; }
.block__head { display: flex; align-items: baseline; justify-content: space-between; gap: .5rem 1rem; flex-wrap: wrap; }
.split { display: grid; grid-template-columns: 1fr 1fr; gap: 0 1.25rem; align-items: start; }
.split > .block { margin-top: 1.25rem; }
.link-btn {
background: none; border: 0; padding: .2rem 0; cursor: pointer;
color: #1F5FA8; font-weight: 600; font-size: .9rem;
text-decoration: underline; text-underline-offset: 3px;
}
.link-btn:hover { color: var(--navy); }

/* ---------- Hero ---------- */
.hero {
display: grid; grid-template-columns: minmax(0, 1fr) auto; gap: 1rem 2.5rem;
background: #DCEAF9; border-radius: var(--r-l);
padding: clamp(1.25rem, 3vw, 2.25rem) clamp(1.25rem, 3vw, 2.5rem) 1.25rem;
}
.hero__title { font-size: clamp(1.6rem, 1.2rem + 1.6vw, 2.35rem); margin-bottom: .6rem; }
.hero__objective {
font-family: var(--font-serif); font-size: clamp(1rem, .95rem + .3vw, 1.15rem);
line-height: 1.75; max-width: 64ch; color: var(--ink);
}
.hero__context { color: var(--ink-2); max-width: 72ch; font-size: .95rem; margin: 0; }
.hero__rail { grid-column: 1 / -1; }

.countdown { display: flex; flex-direction: column; align-items: flex-end; text-align: right; min-width: 11rem; }
.countdown__num {
font-family: var(--font-serif); font-weight: 700; color: var(--navy);
font-size: clamp(3rem, 2rem + 4vw, 4.75rem); line-height: 1; letter-spacing: -.02em;
}
.countdown__txt { font-weight: 600; color: var(--navy); margin-top: .35rem; max-width: 12rem; line-height: 1.3; }
.countdown__date { color: var(--ink-2); font-size: .9rem; margin-top: .2rem; }

/* Ruta de hitos */
.rail { position: relative; height: 10rem; margin-top: 1.25rem; }
.rail__line {
position: absolute; left: 0; right: 0; top: 7.25rem; height: 4px;
background: #B7D0EE; border-radius: 2px;
}
.rail__fill {
position: absolute; left: 0; top: 0; bottom: 0; width: var(--hoy, 0%);
background: var(--navy); border-radius: 2px;
animation: rail-fill 1.2s cubic-bezier(.2, .7, .2, 1) both;
}
@keyframes rail-fill { from { width: 0; } }
.rail__today { position: absolute; top: 7.25rem; left: var(--hoy, 0%); width: 0; height: 0; }
.rail__today::before {
content: ""; position: absolute; left: -1px; top: -.55rem;
width: 2px; height: calc(1.1rem + 4px); background: var(--ember);
}
.rail__today span {
position: absolute; top: 1.05rem; left: 0; transform: translateX(-50%);
white-space: nowrap; font-size: .8rem; font-weight: 700; color: #B63E0F;
}
.rail__today.is-start span { transform: none; left: -.25rem; }
.rail__today.is-end span { transform: translateX(-100%); left: .25rem; }
.rail__stops { list-style: none; margin: 0; padding: 0; }
.rail__stop { position: absolute; top: calc(7.25rem + 2px); left: var(--pos); width: 0; height: 0; }
.rail__dot {
position: absolute; left: 0; top: 0; width: 14px; height: 14px;
transform: translate(-50%, -50%); border-radius: 50%;
background: var(--surface); border: 3px solid var(--navy);
}
.rail__stop.is-done .rail__dot { background: var(--navy); }
.rail__stop.is-key .rail__dot { width: 20px; height: 20px; background: var(--ember); border-color: var(--ember); }
.rail__label {
position: absolute; left: 0; transform: translateX(-50%);
width: 9.5rem; text-align: center; font-size: .8125rem; line-height: 1.3; color: var(--ink-2);
}
.rail__label strong { display: block; color: var(--navy); font-size: .875rem; }
.rail__stop.tier-a .rail__label { bottom: 1rem; }
.rail__stop.tier-b .rail__label { bottom: 3.75rem; }
.rail__stop.tier-b::before {
content: ""; position: absolute; left: 0; bottom: .6rem;
width: 1px; height: 3rem; background: var(--sky);
}
.rail__stop.is-start .rail__label { transform: none; left: -.5rem; text-align: left; }
.rail__stop.is-end .rail__label { transform: none; left: auto; right: -.5rem; text-align: right; }
.rail__stop.is-key .rail__label strong { color: #B63E0F; }
.rail__now { display: none; }

/* ---------- Estados, etiquetas y chips ---------- */
.status {
display: inline-flex; align-items: center; gap: .35rem; white-space: nowrap;
font-size: .8125rem; font-weight: 600; line-height: 1.2;
padding: .28rem .6rem; border-radius: 999px;
}
.status::before { content: ""; width: .45rem; height: .45rem; border-radius: 50%; background: currentColor; flex: none; }
.status--hecho { background: #E2F0DF; color: #2C6526; }
.status--curso { background: #DCEAF9; color: #1D4F8C; }
.status--pronto { background: #FFEFC7; color: #734A00; }
.status--vencida { background: #FBE1DA; color: #962711; }
.status--programada { background: #EDF2F8; color: #3F4F68; }
.status--sinfecha, .status--continuo { background: #EEF1F5; color: #4F5E73; }
.status--decision { background: #FFF4DA; color: #6E4A00; }

.tag { display: inline-flex; align-items: center; gap: .4rem; font-size: .8125rem; font-weight: 600; color: var(--ink-2); }
.tag::before, .dot {
content: ""; display: inline-block; flex: none;
width: .6rem; height: .6rem; border-radius: 2px; background: var(--mesa, var(--sky));
}

.filters { display: flex; flex-wrap: wrap; gap: .5rem; margin: .25rem 0 1rem; }
.chip-btn {
display: inline-flex; align-items: center; gap: .45rem; cursor: pointer;
border: 1px solid var(--line); background: var(--surface); color: var(--navy);
font-weight: 600; font-size: .9rem; padding: .4rem .85rem; border-radius: 999px;
}
.chip-btn[data-mesa]::before {
content: ""; width: .55rem; height: .55rem; border-radius: 2px; background: var(--mesa);
}
.chip-btn:hover { border-color: var(--sky); }
.chip-btn[aria-pressed="true"] { background: var(--navy); border-color: var(--navy); color: #fff; }
.chip-btn__n { font-weight: 400; opacity: .8; }

/* ---------- Decisiones y alertas ---------- */
.decisions, .alerts { list-style: none; padding: 0; }
.decisions li, .alerts li { padding: .85rem 0; border-top: 1px solid var(--line-soft); }
.decisions li:first-child, .alerts li:first-child { border-top: 0; padding-top: .25rem; }
.decision__head, .alert__head { display: flex; align-items: flex-start; justify-content: space-between; gap: .75rem; }
.decision__title, .alert__title { font-weight: 700; color: var(--navy); margin: 0 0 .2rem; }
.decision__text, .alert__text { margin: 0; color: var(--ink-2); font-size: .95rem; }
.alert__src { margin: .3rem 0 0; font-size: .8125rem; color: var(--ink-3); }
.level { flex: none; font-size: .75rem; font-weight: 700; padding: .2rem .55rem; border-radius: 999px; }
.level--alto { background: #FBE1DA; color: #962711; }
.level--medio { background: #FFEFC7; color: #734A00; }

/* ---------- Actividades ---------- */
.acts { list-style: none; padding: 0; }
.act {
display: grid; grid-template-columns: 3.6rem minmax(0, 1fr) auto; gap: .9rem; align-items: start;
padding: .9rem 0; border-top: 1px solid var(--line-soft);
}
.acts > .act:first-child { border-top: 0; }
.act__date {
display: flex; flex-direction: column; align-items: center; justify-content: center;
background: var(--mist); border-radius: var(--r-s); padding: .35rem .2rem;
color: var(--navy); line-height: 1.1; text-align: center; min-height: 3.4rem;
border-left: 3px solid var(--mesa, var(--sky));
}
.act__day { font-family: var(--font-serif); font-weight: 700; font-size: 1.25rem; }
.act__mon { font-size: .75rem; font-weight: 600; }
.act__year { font-size: .6875rem; color: var(--ink-3); }
.act__nodate { font-size: .75rem; font-weight: 600; color: var(--ink-3); }
.act__title { margin: 0 0 .3rem; font-weight: 600; color: var(--ink); }
.act__ref {
display: inline-block; margin-right: .4rem; padding: .05rem .4rem; border-radius: 4px;
background: var(--navy); color: #fff; font-size: .75rem; font-weight: 700; vertical-align: .1em;
}
.act__meta { display: flex; flex-wrap: wrap; gap: .2rem 1rem; margin: 0; font-size: .875rem; color: var(--ink-2); }
.act__src { margin: .3rem 0 0; font-size: .8125rem; color: var(--ink-3); }
.act--vencida .act__date { background: #FBE9E4; }
.act--hecho .act__title { color: var(--ink-2); }
.empty { color: var(--ink-3); font-style: italic; padding: .75rem 0; }

.cols { display: grid; grid-template-columns: 1.25fr 1fr; gap: 1.25rem; align-items: start; }
.cols > section {
background: var(--surface); border: 1px solid var(--line); border-radius: var(--r-m);
padding: clamp(1rem, 2vw, 1.5rem);
}
.counts { list-style: none; padding: 0; margin: 0 0 1rem; display: flex; flex-wrap: wrap; gap: .5rem; }
.counts li {
display: flex; align-items: baseline; gap: .4rem; background: var(--surface);
border: 1px solid var(--line); border-radius: var(--r-s); padding: .45rem .8rem; font-size: .9rem; color: var(--ink-2);
}
.counts strong { font-family: var(--font-serif); font-size: 1.15rem; color: var(--navy); }
.counts .is-alert strong { color: #962711; }

.overdue {
background: #FDF1ED; border: 1px solid #F3C9BC; border-radius: var(--r-s);
padding: .75rem 1rem; margin-bottom: 1rem;
}
.overdue p { margin: 0 0 .25rem; font-weight: 600; color: #7A2210; }
.overdue ul { padding-left: 1.1rem; font-size: .9rem; color: var(--ink-2); }

.edt { list-style: none; padding: 0; columns: 2 22rem; column-gap: 2rem; }
.edt li { break-inside: avoid; display: grid; grid-template-columns: 3rem 1fr; gap: .5rem; padding: .45rem 0; border-top: 1px solid var(--line-soft); }
.edt__code { font-weight: 700; color: var(--m-ordenante); font-variant-numeric: tabular-nums; }
.edt__note { display: block; font-size: .85rem; color: #734A00; margin-top: .15rem; }
.edt li.is-sub { padding-left: 1rem; }

/* ---------- Estructura (organigrama) ---------- */
.org { margin-top: 1rem; }
.org__top {
background: var(--surface); border: 1px solid var(--line); border-top: 5px solid var(--mesa);
border-radius: var(--r-m); padding: 1rem 1.25rem; max-width: 46rem; margin: 0 auto; text-align: center;
}
.org__top h4 { margin-bottom: .3rem; }
.org__top p { margin: 0 0 .3rem; color: var(--ink-2); font-size: .95rem; }
.org__top .org__meta { font-size: .875rem; color: var(--ink-3); margin: 0; }
.org__roles {
list-style: none; padding: 1rem 0; margin: 0 auto; display: flex; flex-wrap: wrap;
justify-content: center; gap: .5rem; position: relative;
}
.org__roles::before {
content: ""; position: absolute; left: 50%; top: 0; bottom: 0; width: 2px;
background: var(--sky-2); z-index: 0;
}
.org__roles li {
position: relative; z-index: 1; background: var(--mist); border: 1px solid var(--line);
border-radius: 999px; padding: .35rem .9rem; font-size: .875rem; color: var(--ink-2);
}
.org__roles strong { color: var(--navy); margin-left: .3rem; }
.org__mesas { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 1rem; position: relative; padding-top: 1rem; }
.org__mesas::before {
content: ""; position: absolute; top: 0; left: 16.66%; right: 16.66%; height: 2px; background: var(--sky-2);
}
.mesa {
position: relative; background: var(--ice); border: 1px solid var(--line);
border-radius: var(--r-m); padding: 1rem 1.1rem 1.1rem; display: flex; flex-direction: column;
}
.mesa::before {
content: ""; position: absolute; top: calc(-1rem - 1px); left: 50%; width: 2px; height: 1rem; background: var(--sky-2);
}
.mesa__head { border-bottom: 3px solid var(--mesa); padding-bottom: .5rem; margin-bottom: .6rem; }
.mesa__head h4 { margin: 0; font-size: 1.15rem; }
.mesa__sub { margin: .1rem 0 0; font-size: .875rem; color: var(--ink-3); }
.mesa__facts { display: flex; flex-wrap: wrap; gap: .25rem 1.25rem; margin: 0 0 .6rem; font-size: .875rem; }
.mesa__facts div { display: flex; gap: .3rem; }
.mesa__facts dt { color: var(--ink-3); }
.mesa__facts dd { margin: 0; font-weight: 600; color: var(--navy); }
.mesa__obj { font-family: var(--font-serif); font-size: .95rem; line-height: 1.65; color: var(--ink); }
.mesa h5 { font-family: var(--font-sans); font-size: .875rem; color: var(--ink-2); margin: .25rem 0 .35rem; }
.mesa__list { padding-left: 1.1rem; font-size: .9rem; color: var(--ink); }
.mesa__list li { margin-bottom: .3rem; }
.mesa__list li::marker { color: var(--mesa); }
.mesa__foot {
margin-top: auto; padding-top: .75rem; display: flex; flex-wrap: wrap; gap: .4rem;
}
.mesa__pill {
font-size: .8125rem; background: var(--surface); border: 1px solid var(--line);
border-radius: 999px; padding: .2rem .65rem; color: var(--ink-2);
}
.mesa__pill strong { color: var(--navy); }
.mesa details { margin-top: .75rem; font-size: .875rem; color: var(--ink-2); }
.mesa summary { cursor: pointer; font-weight: 600; color: #1F5FA8; }
.mesa details p { margin: .4rem 0 0; }

/* ---------- Arranque y mercado ---------- */
.progress-list { list-style: none; padding: 0; }
.progress-list li { display: grid; grid-template-columns: minmax(0, 1fr) 7rem; gap: .25rem 1rem; padding: .5rem 0; align-items: center; }
.progress-list .pl__name { font-weight: 600; font-size: .95rem; }
.progress-list .pl__val { text-align: right; font-weight: 700; color: var(--navy); font-variant-numeric: tabular-nums; }
.bar { grid-column: 1 / -1; height: 8px; background: var(--mist); border-radius: 999px; overflow: hidden; }
.bar span { display: block; height: 100%; background: var(--sky); border-radius: 999px; }
.bar span.is-full { background: var(--navy); }

.figures { display: grid; grid-template-columns: 1fr 1fr; gap: .75rem 1.25rem; margin: .5rem 0 1rem; }
.figure__val { display: block; font-family: var(--font-serif); font-weight: 700; font-size: 1.5rem; color: var(--navy); line-height: 1.2; }
.figure__txt { display: block; font-size: .875rem; color: var(--ink-2); line-height: 1.4; }
.phases { list-style: none; padding: 0; counter-reset: fase; display: grid; gap: .5rem; }
.phases li {
counter-increment: fase; display: grid; grid-template-columns: 1.9rem 1fr; gap: .6rem; align-items: start;
font-size: .9rem; color: var(--ink-2);
}
.phases li::before {
content: counter(fase); display: grid; place-items: center; width: 1.7rem; height: 1.7rem;
border-radius: 50%; background: var(--sky-3); color: var(--navy); font-weight: 700; font-size: .85rem;
}
.phases strong { color: var(--navy); }
.subhead { font-family: var(--font-sans); font-size: .9rem; color: var(--ink-2); margin: 1rem 0 .5rem; }
.source { font-size: .8125rem; color: var(--ink-3); margin: 1rem 0 0; }

/* ---------- Resumen semanal ---------- */
.now-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr)); gap: 1rem 1.5rem; margin-top: .5rem; }
.now-col h4 { font-family: var(--font-sans); font-size: .95rem; margin-bottom: .35rem; }
.now-col ul { list-style: none; padding: 0; }
.now-col li { padding: .4rem 0; border-top: 1px solid var(--line); font-size: .9rem; }
.now-col li:first-child { border-top: 0; }
.now-col .when { display: block; font-size: .8125rem; color: var(--ink-3); }

.weeks { display: grid; grid-template-columns: 17rem minmax(0, 1fr); gap: 1.25rem; margin-top: 1.25rem; align-items: start; }
.weeks__index ul { list-style: none; padding: 0; display: grid; gap: .5rem; position: sticky; top: 4.5rem; }
.week-btn {
width: 100%; text-align: left; cursor: pointer; background: var(--surface);
border: 1px solid var(--line); border-radius: var(--r-m); padding: .75rem .9rem; color: var(--ink);
}
.week-btn:hover { border-color: var(--sky); }
.week-btn[aria-current="true"] { background: var(--mist); border-color: var(--sky); box-shadow: inset 4px 0 0 var(--navy); }
.week-btn__range { display: block; font-weight: 700; color: var(--navy); }
.week-btn__head {
display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;
font-size: .85rem; color: var(--ink-2); margin-top: .15rem;
}
.weeks__detail {
background: var(--surface); border: 1px solid var(--line); border-radius: var(--r-m);
padding: clamp(1rem, 2.5vw, 1.75rem);
}
.week__lead { font-family: var(--font-serif); font-size: 1.05rem; line-height: 1.7; color: var(--ink); max-width: 68ch; }
.week__sessions { list-style: none; padding: 0; display: flex; flex-wrap: wrap; gap: .5rem; margin: 0 0 1.25rem; }
.week__sessions li {
display: inline-flex; align-items: center; gap: .4rem; font-size: .85rem; font-weight: 600;
background: var(--mist); border-radius: 999px; padding: .25rem .75rem; color: var(--navy);
}
.week__groups { display: grid; gap: 1rem; margin-bottom: 1.25rem; }
.week__group { border-left: 3px solid var(--mesa); padding-left: 1rem; }
.week__group h5 { display: flex; align-items: center; gap: .45rem; font-family: var(--font-sans); font-size: .95rem; margin-bottom: .3rem; }
.week__group ul, .week__list ul { padding-left: 1.1rem; }
.week__group li, .week__list li { margin-bottom: .4rem; max-width: 75ch; }
.week__list h4 { font-family: var(--font-sans); font-size: 1rem; margin: 1rem 0 .4rem; }
.week__list--alert li::marker { color: var(--ember); }
.week__src { font-size: .8125rem; color: var(--ink-3); margin: 1.25rem 0 0; }
.split--tight { gap: 0 2rem; }

/* ---------- Gantt ---------- */
.legend { list-style: none; padding: 0; display: flex; flex-wrap: wrap; gap: .4rem 1.1rem; font-size: .85rem; color: var(--ink-2); margin: 0 0 .75rem; }
.legend li { display: inline-flex; align-items: center; gap: .4rem; }
.sw { width: 1.1rem; height: .65rem; border-radius: 3px; display: inline-block; }
.sw--ms { width: .65rem; height: .65rem; transform: rotate(45deg); border-radius: 1px; }
.sw--today { width: 2px; height: .95rem; border-radius: 0; background: var(--ember); }
.sw.ms-futuro { background: var(--navy); }
.sw.ms-key { background: var(--ember); }

.gantt { --lab: 17rem; background: var(--surface); border: 1px solid var(--line); border-radius: var(--r-m); overflow: hidden; }
.gantt__scroll { overflow-x: auto; overscroll-behavior-x: contain; }
.gantt__inner { min-width: var(--g-min, 900px); }
.g-row { display: grid; grid-template-columns: var(--lab) minmax(0, 1fr) 16px; min-height: 2.7rem; border-top: 1px solid var(--line-soft); }
.g-lab {
position: sticky; left: 0; z-index: 3; background: var(--surface);
padding: .4rem .75rem; border-right: 1px solid var(--line);
display: flex; flex-direction: column; justify-content: center; font-size: .875rem; line-height: 1.25;
}
.g-name { font-weight: 600; color: var(--ink); }
.g-dates { font-size: .75rem; color: var(--ink-3); margin-top: .1rem; }
.g-track { position: relative; }
.g-body { position: relative; }
.g-grid { position: absolute; top: 0; bottom: 0; left: var(--lab); right: 16px; pointer-events: none; z-index: 1; }
.g-line { position: absolute; top: 0; bottom: 0; width: 1px; background: var(--line-soft); }
.g-today { position: absolute; top: 0; bottom: 0; width: 2px; margin-left: -1px; background: var(--ember); z-index: 2; }
.g-row--head { background: var(--mist); border-top: 0; min-height: 3rem; }
.g-row--head .g-lab { background: var(--mist); font-weight: 700; color: var(--navy); }
.g-month {
position: absolute; top: 0; bottom: 0; padding-top: .45rem; text-align: center;
font-size: .75rem; font-weight: 700; color: var(--navy); border-left: 1px solid var(--line); white-space: nowrap; overflow: hidden;
}
.g-month:first-child { border-left: 0; }
.g-hoy {
position: absolute; bottom: .3rem; transform: translateX(-50%); z-index: 4;
background: var(--ember); color: #fff; font-size: .6875rem; font-weight: 700;
padding: .05rem .45rem; border-radius: 999px; white-space: nowrap;
}
.g-row--group { background: #F5F9FE; min-height: 2.3rem; }
.g-row--group .g-lab {
background: #F5F9FE; flex-direction: row; align-items: center; justify-content: flex-start;
gap: .5rem; font-family: var(--font-serif); font-weight: 700; color: var(--navy); font-size: .9rem;
}
.g-note { display: block; font-family: var(--font-sans); font-weight: 400; font-size: .75rem; color: var(--ink-3); }
.g-group-text { display: flex; flex-direction: column; }
.g-bar {
position: absolute; top: 50%; height: 1rem; transform: translateY(-50%); z-index: 3;
border-radius: 4px; background: var(--sky-2); min-width: 4px;
}
.g-ms {
position: absolute; top: 50%; width: .85rem; height: .85rem; z-index: 3;
transform: translate(-50%, -50%) rotate(45deg); background: var(--navy); border-radius: 1px;
}
.g-row.is-current .g-lab { box-shadow: inset 3px 0 0 var(--ember); }
.g-row.is-current .g-name { color: var(--navy); }

/* Colores por estado (timeline del proyecto) */
.st-futuro { background: var(--sky-2); }
.st-curso { background: #4F86C6; }
.st-hecho { background: #7DB56F; }
.st-pasado { background: #B6C3D4; }
.g-ms.st-futuro { background: var(--navy); }
.g-ms.st-curso { background: #4F86C6; }
.g-ms.is-key { background: var(--ember); width: 1rem; height: 1rem; }

/* Colores por categoría (roadmap CECOBAN) */
.cat-definicion { background: #3F72B0; }
.cat-tecnologia { background: #6FA3DC; }
.cat-conectividad { background: #6FAA4C; }
.cat-legal { background: #9FA8B3; }
.cat-limite { background: #C0392B; }
.cat-entrega { background: #EDB61F; }
.cat-preparacion { background: #EE8434; }
.cat-arranque { background: #1E9E4F; }
.g-bar.is-past, .g-ms.is-past { opacity: .5; }

/* ---------- Tablas ---------- */
.table-wrap { overflow-x: auto; border: 1px solid var(--line); border-radius: var(--r-m); background: var(--surface); margin-top: .5rem; }
table { border-collapse: collapse; width: 100%; font-size: .9rem; }
caption { text-align: left; padding: .6rem .75rem; color: var(--ink-3); font-size: .85rem; }
th, td { text-align: left; padding: .6rem .75rem; border-bottom: 1px solid var(--line-soft); vertical-align: top; }
thead th { background: var(--mist); color: var(--navy); font-weight: 700; white-space: nowrap; }
tbody tr:last-child td, tbody tr:last-child th { border-bottom: 0; }
tbody tr:hover { background: #F7FAFE; }
td.num, th.num { text-align: right; font-variant-numeric: tabular-nums; }
td.center, th.center { text-align: center; }
.t-min { min-width: 16rem; }
tr.is-current td, tr.is-current th { background: #FFF8F4; }
tr.group-row th { background: #F5F9FE; font-family: var(--font-serif); color: var(--navy); }

.risk { display: inline-block; font-size: .75rem; font-weight: 700; padding: .2rem .55rem; border-radius: 999px; white-space: nowrap; }
.risk--critico { background: #FBE1DA; color: #962711; }
.risk--alto { background: #FFE6CC; color: #7F4200; }
.risk--medio-alto { background: #FFEFC7; color: #6E4A00; }
.risk--medio { background: #FFF6D6; color: #5E4B00; }
.risk--bajo { background: #E2F0DF; color: #2C6526; }
.risk-defs { display: grid; grid-template-columns: repeat(auto-fit, minmax(14rem, 1fr)); gap: .75rem 1.25rem; margin: 1rem 0 0; font-size: .875rem; }
.risk-defs div { background: var(--ice); border-radius: var(--r-s); padding: .6rem .8rem; }
.risk-defs dt { margin-bottom: .25rem; }
.risk-defs dd { margin: 0; color: var(--ink-2); }

.part { display: inline-block; width: .8rem; height: .8rem; border-radius: 50%; border: 2px solid var(--line); }
.part.is-on { border-color: var(--mesa); background: var(--mesa); }

/* ---------- Regulatorio ---------- */
.defs { margin: 0; }
.defs div { padding: .6rem 0; border-top: 1px solid var(--line-soft); }
.defs div:first-child { border-top: 0; padding-top: 0; }
.defs dt { font-weight: 700; color: var(--navy); }
.defs dd { margin: .15rem 0 0; color: var(--ink-2); font-size: .95rem; }
.deadlines { list-style: none; padding: 0; }
.deadlines li { padding: .65rem 0 .65rem .9rem; border-left: 3px solid var(--sky); margin-bottom: .5rem; background: var(--ice); border-radius: 0 var(--r-s) var(--r-s) 0; }
.deadlines strong { display: block; color: var(--navy); }
.deadlines span { display: block; font-size: .9rem; color: var(--ink-2); }
.deadlines small { color: var(--ink-3); }
.checklist { list-style: none; padding: 0; display: grid; grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr)); gap: .75rem; }
.checklist li {
background: var(--ice); border: 1px solid var(--line); border-radius: var(--r-s);
padding: .75rem .9rem; font-weight: 600; color: var(--navy); font-size: .95rem;
}

/* ---------- Flujo CECOBAN ---------- */
.flow { display: grid; grid-template-columns: 1fr 1.1fr 1fr; gap: 0; margin-top: .75rem; border: 1px solid var(--line); border-radius: var(--r-m); overflow: hidden; }
.flow__h { padding: .65rem .9rem; font-weight: 700; font-size: .9rem; text-align: center; }
.flow__h--otorgante { background: #E3F0E0; color: #245C22; }
.flow__h--cecoban { background: var(--navy); color: #fff; }
.flow__h--ordenante { background: #EAF4D9; color: #3F5F12; }
.flow__cell { padding: .75rem .9rem; border-top: 1px solid var(--line-soft); font-size: .9rem; display: flex; gap: .6rem; align-items: flex-start; }
.flow__cell--cecoban { background: var(--mist); color: var(--navy); font-weight: 600; justify-content: center; text-align: center; }
.flow__n {
flex: none; display: grid; place-items: center; width: 1.6rem; height: 1.6rem; border-radius: 50%;
background: var(--navy); color: #fff; font-weight: 700; font-size: .8rem;
}
.flow__arrow { color: var(--sky); font-weight: 700; }
.flow__close {
grid-column: 1 / -1; background: #F1ECFA; color: #3F2B6E; padding: .8rem 1rem;
font-size: .9rem; border-top: 1px solid var(--line-soft); text-align: center;
}
.flow__label { display: none; font-size: .75rem; font-weight: 700; color: var(--ink-3); }

.steps { list-style: none; padding: 0; display: grid; grid-template-columns: repeat(6, minmax(0, 1fr)); gap: .6rem; counter-reset: paso; margin-top: .75rem; }
.steps li {
counter-increment: paso; position: relative; background: var(--ice); border: 1px solid var(--line);
border-top: 4px solid var(--sky); border-radius: var(--r-s); padding: .75rem .8rem; font-size: .875rem; color: var(--ink-2);
}
.steps li::before { content: counter(paso, decimal-leading-zero); display: block; font-family: var(--font-serif); font-weight: 700; color: var(--sky); font-size: 1.1rem; }
.steps strong { display: block; color: var(--navy); margin: .15rem 0 .3rem; font-size: .95rem; }

/* ---------- Hitos ---------- */
.milestones { list-style: none; padding: 0; display: grid; grid-template-columns: repeat(auto-fit, minmax(12rem, 1fr)); gap: .75rem; }
.milestones li { background: var(--ice); border: 1px solid var(--line); border-radius: var(--r-s); padding: .75rem .9rem; }
.milestones .ms-date { display: block; font-family: var(--font-serif); font-weight: 700; color: var(--navy); }
.milestones .ms-title { display: block; margin: .15rem 0 .4rem; color: var(--ink); font-weight: 600; }
.milestones li.is-key { border-color: #F3C9BC; background: #FDF3EE; }

/* ---------- Equipo ---------- */
.roles { list-style: none; padding: 0; display: grid; grid-template-columns: repeat(auto-fit, minmax(16rem, 1fr)); gap: .75rem; }
.roles li { border: 1px solid var(--line); border-radius: var(--r-s); padding: .8rem .9rem; background: var(--ice); }
.roles .r-rol { display: block; font-size: .8125rem; font-weight: 600; color: var(--ink-3); }
.roles .r-name { display: block; font-family: var(--font-serif); font-weight: 700; color: var(--navy); margin: .1rem 0 .3rem; }
.roles p { margin: 0; font-size: .875rem; color: var(--ink-2); }
.week-strip { display: grid; grid-template-columns: repeat(5, minmax(0, 1fr)); gap: .5rem; margin: .5rem 0 .75rem; }
.week-strip div { border-radius: var(--r-s); padding: .7rem .6rem; text-align: center; font-size: .875rem; border: 1px solid var(--line); background: var(--ice); color: var(--ink-3); }
.week-strip div.is-on { background: var(--navy); color: #fff; border-color: var(--navy); }
.week-strip strong { display: block; font-size: .95rem; }
.week-strip div.is-on strong { color: #fff; }
.decision-bar { background: var(--brick); color: #fff; border-radius: var(--r-s); padding: .6rem .9rem; font-size: .9rem; font-weight: 600; }

.res-summary { display: grid; grid-template-columns: auto 1fr; gap: .5rem 2rem; align-items: center; margin-bottom: 1rem; }
.res-total { font-family: var(--font-serif); font-weight: 700; font-size: 3rem; line-height: 1; color: var(--navy); }
.res-total small { display: block; font-family: var(--font-sans); font-size: .9rem; font-weight: 600; color: var(--ink-2); margin-top: .3rem; }
.stack { display: flex; height: 14px; border-radius: 999px; overflow: hidden; background: var(--mist); }
.stack span { display: block; height: 100%; }
.stack .t-estructural { background: var(--sky); }
.stack .t-operacional { background: var(--navy); }
.stack .t-implementacion { background: var(--ember); }
.res-legend { list-style: none; padding: 0; display: flex; flex-wrap: wrap; gap: .3rem 1.2rem; font-size: .875rem; color: var(--ink-2); margin-top: .5rem; }
.res-legend li { display: inline-flex; align-items: center; gap: .35rem; }
.res-legend .sw.t-estructural { background: var(--sky); }
.res-legend .sw.t-operacional { background: var(--navy); }
.res-legend .sw.t-implementacion { background: var(--ember); }
.res-bymesa { display: flex; flex-wrap: wrap; gap: .4rem 1.25rem; margin-top: .6rem; font-size: .9rem; }
.res-bymesa span { display: inline-flex; align-items: center; gap: .4rem; }
.res-note { margin-top: 1rem; background: var(--ice); border: 1px dashed var(--sky-2); border-radius: var(--r-s); padding: .75rem .9rem; font-size: .9rem; color: var(--ink-2); }
.res-note strong { color: var(--navy); }

.consultant { list-style: none; padding: 0; }
.consultant li { display: flex; justify-content: space-between; gap: 1rem; align-items: flex-start; padding: .6rem 0; border-top: 1px solid var(--line-soft); }
.consultant li:first-child { border-top: 0; }
.consultant p { margin: .15rem 0 0; font-size: .875rem; color: var(--ink-2); }
.docs { display: grid; gap: .75rem; }
.docs section { border: 1px solid var(--line); border-radius: var(--r-s); padding: .7rem .9rem; background: var(--ice); }
.docs h4 { font-family: var(--font-sans); font-size: .95rem; margin: 0 0 .3rem; }
.docs ul { padding-left: 1.1rem; font-size: .875rem; color: var(--ink-2); margin-bottom: .4rem; }
.docs a { font-weight: 600; font-size: .875rem; }

/* ---------- Pie ---------- */
.footer { border-top: 1px solid var(--line); background: var(--mist); color: var(--ink-3); font-size: .85rem; padding: 1.25rem 0 2rem; }
.footer p { margin: 0; max-width: 90ch; }

/* ---------- Responsivo ---------- */
@media (max-width: 1100px) {
.steps { grid-template-columns: repeat(3, minmax(0, 1fr)); }
}
@media (max-width: 960px) {
.split, .cols, .weeks { grid-template-columns: 1fr; }
.weeks__index ul { position: static; grid-template-columns: repeat(auto-fit, minmax(13rem, 1fr)); }
.org__mesas { grid-template-columns: 1fr; }
.org__mesas::before, .mesa::before, .org__roles::before { display: none; }
}
@media (max-width: 720px) {
.masthead__meta p { text-align: left; }
.hero { grid-template-columns: 1fr; }
.countdown { align-items: flex-start; text-align: left; order: -1; flex-direction: row; flex-wrap: wrap; align-items: baseline; gap: .25rem .6rem; }
.countdown__txt { max-width: none; margin-top: 0; }
.countdown__date { flex-basis: 100%; margin-top: 0; }

.rail { height: auto; margin-top: .75rem; }
.rail__line, .rail__today { display: none; }
.rail__stops { border-left: 3px solid #B7D0EE; margin-left: .5rem; }
.rail__stop, .rail__now { position: relative; top: auto; left: auto; width: auto; height: auto; padding: 0 0 .9rem 1.25rem; }
.rail__stop::before { display: none; }
.rail__dot { left: -1.5px; top: .65rem; }
.rail__label, .rail__stop.is-start .rail__label, .rail__stop.is-end .rail__label {
position: static; transform: none; width: auto; text-align: left;
}
.rail__label strong { display: inline; margin-right: .4rem; }
.rail__stops > li:last-child { padding-bottom: 0; }
.rail__now { display: block; font-size: .85rem; font-weight: 700; color: #B63E0F; }
.rail__now::before {
content: ""; position: absolute; left: -1.5px; top: .6rem; width: 12px; height: 3px;
transform: translate(-50%, -50%); background: var(--ember);
}

.act { grid-template-columns: 3.3rem minmax(0, 1fr); }
.act .status { grid-column: 2; justify-self: start; }
.figures { grid-template-columns: 1fr; }
.gantt { --lab: 11rem; }
.steps { grid-template-columns: 1fr 1fr; }
.week-strip { grid-template-columns: repeat(5, minmax(3.6rem, 1fr)); overflow-x: auto; }
.res-summary { grid-template-columns: 1fr; }

.flow { grid-template-columns: 1fr; }
.flow__h--otorgante, .flow__h--ordenante { display: none; }
.flow__cell { border-top: 0; }
.flow__cell--otorgante { border-top: 1px solid var(--line); }
.flow__cell--cecoban { justify-content: flex-start; text-align: left; }
.flow__label { display: block; }
.flow__arrow { display: none; }
}
@media (max-width: 460px) {
.steps { grid-template-columns: 1fr; }
}

/* ---------- Movimiento reducido ---------- */
@media (prefers-reduced-motion: reduce) {
*, *::before, *::after { animation: none !important; transition: none !important; scroll-behavior: auto !important; }
}

/* ---------- Impresión ---------- */
@media print {
body { background: #fff; font-size: 11pt; }
.tabs-wrap, .skip, #btn-imprimir, .filters, .link-btn { display: none !important; }
.masthead { border-top-width: 3px; }
.block, .hero, .mesa, .cols > section, .weeks__detail { break-inside: avoid; border-color: #C9D6E6; }
.gantt__scroll, .table-wrap { overflow: visible; }
.gantt__inner { min-width: 0; }
.g-lab { position: static; }
.weeks { grid-template-columns: 1fr; }
.weeks__index { display: none; }
.rail__fill { animation: none; }
a { color: inherit; text-decoration: none; }
* { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
}
